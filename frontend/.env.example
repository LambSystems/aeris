# Browser-visible (must be NEXT_PUBLIC_*). Restart dev server after changes.
NEXT_PUBLIC_API_URL=http://localhost:8000
# gemini | anthropic | openai | template — template is fast and needs no API keys
NEXT_PUBLIC_AERIS_PROVIDER=template
